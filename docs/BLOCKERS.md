# Faz 7 blokerleri

- 2026-07-30 — Ne denedim: `docker info` ile yerel Docker daemon bağlantısını sınadım. Ne oldu: `dockerDesktopLinuxEngine` named pipe bulunamadı. Neden devam edemedim: Docker Desktop çalışmıyor. Bağımsız sıradaki iş: ürün kodu, saf mantık testleri, compose config ve dokümantasyonu tamamla; uçtan uca konteyner kapısını Docker Desktop başlatıldıktan sonra yeniden çalıştır.
- 2026-07-30 — Ne denedim: GitHub yayınlama ön koşulu olarak `gh --version`/`gh auth status`. Ne oldu: `gh` kurulu değil. Neden devam edemedim: draft PR akışı kullanılamıyor. Bağımsız sıradaki iş: kullanıcının istediği doğrudan `git push origin main` akışını kullan; PR açma.

