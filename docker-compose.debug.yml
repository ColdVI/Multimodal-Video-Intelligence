services:
  pg:
    ports:
      - "${BIND_HOST:-127.0.0.1}:5442:5432"
  ch:
    ports:
      - "${BIND_HOST:-127.0.0.1}:8143:8123"
      - "${BIND_HOST:-127.0.0.1}:9100:9000"
  qdrant:
    ports:
      - "${BIND_HOST:-127.0.0.1}:6343:6333"
      - "${BIND_HOST:-127.0.0.1}:6344:6334"
